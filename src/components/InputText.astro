---
import type { TRANSITION_AFTER_PREPARATION } from 'astro:transitions/client';

interface Props {
    etiqueta: string;
  
}

//crear una variable 
// para recibir la etiqueta 
// astro.props= los valores enviados del indexx
const {etiqueta} = Astro.props;

---

<!--
etiqueta input :
se utilizan par los siguientes controles:
- campo de testo
-password
-checkbox
-boton de envio
ES ETIQUETA CERRADA 
-->
<label>{etiqueta}:</label>
<input  type="text"/>
<style>
    /*solo se puede ponr aqui 
    /*sinta*/
 input{

    background-color: #00ffff;
    width: 420px;
    height: 20px;;
    color: rgb(0, 0, 0);
    /* bords:
    -color
    -peso(grosor)
    -stilo(tipo de linea)
    */
  border-color: blueviolet;
  border-color: 3px;
  border-style: solid;
  border-radius: 5px;


}

</style>
